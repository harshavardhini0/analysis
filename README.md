# analysis
Creating a Dataset using excel convert the same in to a ‘.csv’ file Upload the Data set using Pandas Explore the data set using ‘numpy’ , ‘pandas’ Libraries
Use : info,describe,sorting,create new colum using the existing columns, group by function, create a new data frame using some of the features in the data frame
